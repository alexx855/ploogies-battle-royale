type WorldMatrix @entity {
  id: ID!
  x: Int!
  y: Int!
  player: Player
  healthAmountToCollect: BigInt!
}

type Player @entity {
  id: ID!
  x: Int!
  y: Int!
  address: Bytes!
  loogieId: BigInt!
  health: BigInt!
  createdAt: BigInt!
  transactionHash: String!
}
